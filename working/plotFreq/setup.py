from setuptools import setup, find_packages

setup(name='plotFreq',
      description='test module for Lecture 08',
      author='Simona Palluotto',
      author_email='s.palluotto@campus.unimib.it',
      version='0.0.2',
      packages=find_packages(),
      install_requires=['numpy', 'matplotlib'])
